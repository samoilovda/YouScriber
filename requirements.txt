# YouScriber - Dependencies

# Core
# Pinned to the version this release was tested against. If YouTube breaks
# downloads, the first fix to try is: pip install -U yt-dlp
yt-dlp==2026.7.4

# Desktop interface (gui.py)
customtkinter>=5.2.2

# Build tool (packages gui.py into a standalone app via YouScriber.spec)
pyinstaller>=6.3.0
